<!-- # Arquivo: .htaccess (Na raiz do projeto)
<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Bloqueia acesso direto APENAS a app e config.
    # Liberamos a pasta 'views' porque seu site navega por dentro dela agora.
    RewriteRule ^(app|config|vendor) - [F,L]
    
    # Impede listagem de diretórios (para ninguém ver seus arquivos listados se digitar só a pasta)
    Options -Indexes
</IfModule> -->